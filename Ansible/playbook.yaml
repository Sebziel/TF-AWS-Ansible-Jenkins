---
- hosts: all
  become: true
  tasks:
  - name: Update apt cache
    apt:
      update_cache: yes
  
  - name: Install Java
    apt:
      name: openjdk-17-jdk
      state: present

  - name: Download Jenkins repository key
    apt_key:
      url: https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key
      state: present
    
  - name: Add Jenkins repository to the sources list
    apt_repository:
      repo: deb http://pkg.jenkins.io/debian-stable binary/
      state: present
      
  - name: Update apt cache
    apt:
      update_cache: yes
  
  - name: Install Jenkins
    apt:
      name: jenkins
      state: present
  
  - name: Ensure /var/jenkins_home exists
    file:
      path: /var/jenkins_home
      state: directory
      owner: jenkins
      group: jenkins
      mode: '0755'
      
  - name: Create directory for Jenkins Groovy init scripts
    file:
      path: /var/lib/jenkins/init.groovy.d
      state: directory
      owner: jenkins
      group: jenkins
      mode: '0755'
      
  - name: Copy script to remote machine
    copy:
      src: JenkinsScripts/CreateAdminUser.groovy
      dest: /var/lib/jenkins/init.groovy.d/CreateAdminUser.groovy
      mode: '0755'
      owner: jenkins
      group: jenkins
      
  - name: Start and enable Jenkins service
    systemd:
      name: jenkins
      state: started
      enabled: true